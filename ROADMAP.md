# ROADMAP

## 分段

| 段 | 一句话 | 怎么算完 |
|---|---|---|
| **一段 · 入口** | 四条业务线各一个入口页，站点可用 | 四个目录都有 `index.md`；线上资源与链接均 200 |
| **二段 · 内容** | 每条业务线下挂真实案例（外部案例优先，没有就用内部案例顶上） | 四条业务线各自至少有 1 个具体案例页，且不含价格与客户名 |
| **三段 · 交付形态** | 自有域名与归档分工成文 | `gallery.quanttide.com` 可访问；案例与归档的边界写成条目 |

## 现在在哪

**一段已过**（2026-09-24）：落地页 `index.md` 按数据 → 课堂 → 云 → 咨询介绍四条业务线；四页入口齐；产品页 URL 由 `index-1` 兜底 slug 修回 `/qtdata`、`/qtclass`、`/qtcloud`、`/qtconsult`；CI 补 `BASE_URL` 后线上资源与站内链接恢复 200。配套补齐了 AGENTS、CONTRIBUTING、STATUS、ROADMAP。

**现在站在二段的起点**：只有教学业务线有具体案例（大数据导论实践课任务书）。数据、云、咨询三条业务线有入口介绍、没有案例——归档站里躺着可用的原件（如 `gallery/qtdata/price-indexer/` 的需求范围、蓝图、报价、交付物），但「把原件压成人能看懂的三五行」这件事还没做。

## 与结构无关的欠账

- `quanttide-tech` 的 CONTRIBUTING 里那条「SKILL 同步到 gallery」已与本仓库定位不符，待在那个仓库改掉
- 内容迁往归档站的规则未成文（迁什么、留什么、谁决定）
- CI 无链接检查与格式检查门禁
- 仓库无 `.gitignore`，本地构建残留 `_build/` 需手工清理

## 待决（不进待办）

- **案例页要不要配图**：现在是纯文字。案例要给人看，图（界面截图、流程图）可能比文字有效，但会让仓库变重、也涉及客户信息脱敏
- **案例与归档的边界画在哪**：归档站已收 `gallery/` 目录（qtdata、qtclass），与本仓库同名同主题——是「原件在归档、介绍在案例」，还是把归档的 `gallery/` 也当案例库的一部分？
